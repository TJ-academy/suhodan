<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orderitem">
	<select id="listByOrderId" resultType="com.example.suhodan.orders.OrderItemDTO">
		select * from suho_order_items
		where order_id = #{order_id}
	</select>
	<insert id="insertItems" parameterType="list">
	  INSERT ALL
	  <foreach collection="list" item="item">
	    INTO suho_order_items (item_id, order_id, product_id, product_name, quantity, unit_price)
	    VALUES (#{item.itemId}, #{item.orderId}, #{item.productId}, #{item.productName}, #{item.quantity}, #{item.unitPrice})
	  </foreach>
	  SELECT * FROM dual
	</insert>


</mapper>